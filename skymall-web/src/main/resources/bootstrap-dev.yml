spring:
  application:
    name: skymallWeb
  cloud:
    config:
      profile: dev
      label: master
      uri: http://127.0.0.1:7070/
      discovery:
        enabled: true
        service-id: SKYMALL-CONFIG
